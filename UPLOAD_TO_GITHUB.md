# 📤 Инструкция по загрузке на GitHub

## ✅ Текущий статус

- ✅ Git репозиторий уже инициализирован
- ✅ `.gitignore` настроен правильно
- ✅ GitHub Actions workflows созданы (`.github/workflows/`)
- ⏳ Есть незакоммиченные файлы (38 файлов)

## 🚀 Быстрая загрузка на GitHub

### Шаг 1: Создать репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Название репозитория: `drip-hosting-bot` (или другое)
3. Описание: "Production-ready Telegram bot with clean architecture"
4. Выберите: **Private** или **Public**
5. **НЕ** добавляйте:
   - ❌ README.md (у вас уже есть)
   - ❌ .gitignore (у вас уже есть)
   - ❌ Лицензию (можно добавить позже)
6. Нажмите **"Create repository"**

### Шаг 2: Загрузить код на GitHub

#### Вариант A: Использовать PowerShell скрипт (Windows) 🤖

```powershell
# Запустить автоматический скрипт
.\setup-github.ps1
```

Скрипт поможет:
- ✅ Добавить все файлы
- ✅ Создать коммит
- ✅ Подключить к GitHub
- ✅ Загрузить код

#### Вариант B: Вручную через терминал 🔧

```bash
# 1. Добавить все файлы
git add .

# 2. Проверить что будет закоммичено (должно быть много файлов)
git status

# 3. Создать коммит
git commit -m "Refactored bot architecture: Clean code structure, TypeScript, Zod validation, repositories, services, payment providers abstraction"

# 4. Добавить remote (замените YOUR_USERNAME на ваш GitHub username)
git remote add origin https://github.com/YOUR_USERNAME/drip-hosting-bot.git

# 5. Переименовать ветку в main (если нужно)
git branch -M main

# 6. Загрузить код на GitHub
git push -u origin main
```

**Если remote уже существует:**
```bash
# Проверить текущий remote
git remote -v

# Если нужно изменить URL:
git remote set-url origin https://github.com/YOUR_USERNAME/drip-hosting-bot.git

# Загрузить код
git push -u origin main
```

### Шаг 3: Проверить что всё загрузилось

1. Откройте ваш GitHub репозиторий в браузере
2. Проверьте, что все файлы на месте:
   - ✅ `src/` папка с кодом
   - ✅ `.github/workflows/` с CI/CD workflows
   - ✅ `package.json`, `README.md`, и т.д.
3. Перейдите в **Actions** вкладку
4. Должен запуститься workflow "CI/CD Pipeline"
5. Дождитесь завершения (зеленая галочка ✅)

### Шаг 4: Настроить Secrets в GitHub

После загрузки кода:

1. Перейдите: **Settings** → **Secrets and variables** → **Actions**
2. Нажмите **"New repository secret"**
3. Добавьте все переменные из вашего `.env` файла:

**Обязательные:**
- `BOT_TOKEN` = токен вашего Telegram бота
- `BOT_USERNAME` = имя бота
- `WEBSITE_URL` = URL вашего сайта
- `SUPPORT_USERNAME_TG` = имя поддержки
- `PAYMENT_AAIO_ID` = ваш AAIO ID
- `PAYMENT_AAIO_SECRET_ONE` = AAIO Secret 1
- `PAYMENT_AAIO_SECRET_TWO` = AAIO Secret 2
- `PAYMENT_AAIO_TOKEN` = AAIO Token
- `PAYMENT_CRYSTALPAY_ID` = CrystalPay ID
- `PAYMENT_CRYSTALPAY_SECRET_ONE` = CrystalPay Secret 1
- `PAYMENT_CRYSTALPAY_SECRET_TWO` = CrystalPay Secret 2
- `VMM_EMAIL` = ваш VMManager email
- `VMM_PASSWORD` = VMManager password
- `VMM_ENDPOINT_URL` = VMManager endpoint URL

**Опциональные (для деплоя):**
- `SSH_HOST`, `SSH_USER`, `SSH_KEY`, `SSH_PORT` - для автоматического деплоя
- `DOCKER_USERNAME`, `DOCKER_PASSWORD` - для публикации Docker образа

## ✅ Что произойдет после настройки

### Автоматически при каждом push:

1. **CI/CD Pipeline запустится:**
   - ✅ Проверит TypeScript компиляцию
   - ✅ Запустит линтер
   - ✅ Проверит форматирование
   - ✅ Соберет проект

2. **Test Bot запустится** (при pull request):
   - ✅ Проверит валидацию конфига
   - ✅ Тестирует сборку
   - ✅ Проверит импорты модулей

3. **Deploy Bot запустится** (при push в main, если настроен):
   - ✅ Соберет deployment package
   - ✅ Задеплоит на сервер
   - ✅ Соберет Docker образ

## 🔍 Мониторинг

### Проверка статуса:

1. **GitHub Actions:**
   - Перейдите в **Actions** вкладку
   - Проверьте последний workflow run
   - Зеленая галочка ✅ = успешно
   - Красный крестик ❌ = ошибка

2. **Логи:**
   - Кликните на workflow run
   - Кликните на job (например, "Test & Build")
   - Просмотрите логи каждого step

## 📋 Пример команд для быстрого старта

```bash
# Всё в одной команде (замените YOUR_USERNAME и YOUR_REPO):
git add . && \
git commit -m "Refactored bot architecture" && \
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git || \
git remote set-url origin https://github.com/YOUR_USERNAME/YOUR_REPO.git && \
git branch -M main && \
git push -u origin main
```

**Или пошагово:**

```bash
# 1. Добавить файлы
git add .

# 2. Проверить
git status

# 3. Закоммитить
git commit -m "Refactored bot architecture: Clean code, TypeScript, Zod, repositories, services"

# 4. Добавить remote (замените YOUR_USERNAME/YOUR_REPO)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# 5. Отправить на GitHub
git push -u origin main
```

## 🎯 Финальный чек-лист

- [ ] Репозиторий создан на GitHub
- [ ] Все файлы добавлены (`git add .`)
- [ ] Коммит создан (`git commit`)
- [ ] Remote настроен (`git remote add origin`)
- [ ] Код отправлен на GitHub (`git push`)
- [ ] Secrets добавлены в GitHub Settings
- [ ] GitHub Actions запустились
- [ ] Все тесты проходят ✅

## 📚 Дополнительные ресурсы

- `GITHUB_SETUP.md` - Детальная инструкция по настройке
- `GITHUB_QUICK_START.md` - Быстрый старт с GitHub
- `GIT_COMMANDS.md` - Полезные Git команды
- `.github/workflows/` - Workflow файлы

## 🚀 Готово!

После выполнения этих шагов ваш проект будет:
- ✅ На GitHub
- ✅ Автоматически тестироваться при каждом push
- ✅ Готов к CI/CD
- ✅ Готов к автоматическому деплою (если настроить)

**Удачи! 🎉**

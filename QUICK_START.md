# 🚀 Быстрый запуск и тестирование

## 📋 Варианты запуска

### Вариант 1: Старый код (index.ts) - уже работает ✅

Этот вариант использует старую структуру, но с новыми улучшениями:

```bash
# Запуск в режиме разработки (с hot-reload)
npm run dev
```

### Вариант 2: Новый код (index.new.ts) - новая архитектура 🆕

Этот вариант использует полностью рефакторенную архитектуру:

```bash
# Временно измените nodemon.json чтобы использовать index.new.ts
# Или запустите напрямую:
npx ts-node src/index.new.ts
```

## 🔧 Пошаговая инструкция

### Шаг 1: Проверка зависимостей

```bash
# Установить зависимости (если ещё не установлены)
npm install
# или
pnpm install
```

### Шаг 2: Проверка .env файла

Убедитесь, что `.env` файл существует и содержит все необходимые переменные:

```bash
# Проверить наличие .env
cat .env
# или в PowerShell:
Get-Content .env
```

Минимально необходимые переменные:
```env
BOT_TOKEN="your_telegram_bot_token"
BOT_USERNAME="your_bot_username"
WEBSITE_URL="https://example.com/"
SUPPORT_USERNAME_TG="support_username"
PAYMENT_AAIO_ID="..."
PAYMENT_AAIO_SECRET_ONE="..."
PAYMENT_AAIO_SECRET_TWO="..."
PAYMENT_AAIO_TOKEN="..."
PAYMENT_CRYSTALPAY_ID="..."
PAYMENT_CRYSTALPAY_SECRET_ONE="..."
PAYMENT_CRYSTALPAY_SECRET_TWO="..."
VMM_EMAIL="email@example.com"
VMM_PASSWORD="password"
VMM_ENDPOINT_URL="https://vm.example.com/"
NODE_ENV="development"
```

### Шаг 3: Запуск проекта

#### Вариант A: Старый код (рекомендуется для начала)

```bash
# Использовать существующий index.ts
npm run dev
```

Вы должны увидеть:
```
[DripHosting Bot]: Starting
[DripHosting Bot]: Started
```

#### Вариант B: Новый код (новая архитектура)

1. Временно переименуйте файлы:
   ```bash
   # Создать backup старого index.ts
   move src\index.ts src\index.old.ts
   
   # Переименовать новый файл
   move src\index.new.ts src\index.ts
   ```

2. Запустить:
   ```bash
   npm run dev
   ```

Вы должны увидеть:
```
[INFO] Initializing bot...
[INFO] Fluent i18n initialized
[INFO] Database initialized
[INFO] VMManager initialized
[INFO] Services initialized
[INFO] PaymentStatusChecker initialized
[INFO] Bot initialized successfully
[INFO] PaymentStatusChecker started
[INFO] ExpirationService started
[INFO] Starting bot...
[INFO] Bot started successfully
```

### Шаг 4: Тестирование

#### 4.1. Базовое тестирование

1. Откройте Telegram и найдите вашего бота
2. Отправьте команду `/start`
3. Должно появиться приветственное сообщение с балансом
4. Проверьте меню - все кнопки должны работать

#### 4.2. Тестирование основных функций

**Профиль:**
- Нажмите "🧑‍💼 Profile" → должна открыться страница профиля
- Нажмите "📤 Top up" → должно открыться меню пополнения

**Пополнение баланса:**
- Выберите сумму (например, "10$")
- Выберите платежную систему (AAIO или CrystalPay)
- Должна появиться ссылка на оплату

**Управление услугами:**
- Нажмите "🛠 Manage services"
- Должно открыться меню управления услугами

**Покупка VDS:**
- Нажмите "💳 Purchase service"
- Выберите "🖥 Virtual Dedicated Server (VDS)"
- Должно открыться меню выбора тарифа VDS

#### 4.3. Тестирование команд (для админов)

```bash
# В Telegram боте (если вы админ):
/start - Главное меню
/help - Список команд админа
/users - Список пользователей (админ/модератор)
/domainrequests - Список запросов доменов (админ/модератор)
/promo_codes - Список промокодов (админ)
```

#### 4.4. Проверка логов

Следите за логами в консоли:

```bash
# Должны быть логи:
[INFO] Database initialized
[INFO] VMManager initialized
[INFO] PaymentStatusChecker started
[INFO] ExpirationService started
```

**Ошибки:**
- Если видите `❌ Invalid environment configuration` → проверьте `.env`
- Если видите `Failed to authenticate with VMManager` → проверьте VMM_EMAIL/VMM_PASSWORD
- Если видите `Payment provider error` → проверьте credentials платежных систем

## 🐛 Решение проблем

### Проблема: "Cannot find module"

```bash
# Переустановить зависимости
rm -rf node_modules
npm install
# или
pnpm install
```

### Проблема: "Invalid environment configuration"

Проверьте `.env` файл - все переменные должны быть заполнены:
```bash
# Проверить валидацию конфига
node -e "require('dotenv').config(); require('./src/app/config.js')"
```

### Проблема: "Database locked"

Закройте другие процессы, использующие базу данных:
```bash
# В Windows - проверьте процессы
tasklist | findstr node
# Закройте лишние процессы
```

### Проблема: "TypeScript compilation errors"

```bash
# Проверить типы
npm run typecheck
# или
npx tsc --noEmit
```

### Проблема: Bot не отвечает

1. Проверьте `BOT_TOKEN` в `.env`
2. Убедитесь, что бот запущен (проверьте логи)
3. Проверьте интернет соединение
4. Проверьте, что бот не заблокирован в Telegram

## 📊 Мониторинг работы бота

### Проверка статуса

**В консоли должны быть:**
- ✅ Регулярные логи о работе сервисов
- ✅ Логи о проверке платежей (каждые 10 секунд)
- ✅ Логи о проверке истечения услуг (каждый день)

**В Telegram:**
- ✅ Бот отвечает на команды
- ✅ Меню работают корректно
- ✅ Сообщения отображаются с правильным форматированием

### Проверка базы данных

```bash
# Проверить размер базы
ls -lh data.db
# или в Windows:
dir data.db

# База должна расти по мере использования
```

### Проверка сессий

```bash
# Проверить папку sessions
ls -la sessions/
# или в Windows:
dir sessions\

# Должны создаваться файлы для каждого пользователя
```

## 🧪 Расширенное тестирование

### Тест платежей (осторожно!)

1. Создайте тестовый счет на небольшую сумму
2. Проверьте, что ссылка на оплату генерируется
3. Проверьте, что после оплаты баланс обновляется (в реальном режиме)

### Тест VDS создания (осторожно!)

1. Убедитесь, что баланс достаточен
2. Попробуйте создать VDS через меню
3. Проверьте, что VDS создается в VMManager
4. Проверьте, что данные сохраняются в базе

### Тест доменов

1. Попробуйте запросить домен
2. Проверьте, что запрос создается в базе
3. Проверьте, что баланс списывается

## 🔄 Переключение между версиями

### Вернуться к старому коду:

```bash
# Если переименовали файлы
move src\index.ts src\index.new.ts
move src\index.old.ts src\index.ts
```

### Использовать новый код:

```bash
# Переименовать
move src\index.ts src\index.old.ts
move src\index.new.ts src\index.ts
```

## 💡 Советы

1. **Начните со старого кода** - он точно работает
2. **Тестируйте новый код параллельно** - используйте два бота
3. **Делайте backup базы** перед тестированием:
   ```bash
   copy data.db data.db.backup
   ```
4. **Следите за логами** - там будет видно все ошибки
5. **Проверяйте баланс** - не тестируйте на реальных деньгах без необходимости

## ✅ Чек-лист запуска

- [ ] Зависимости установлены (`npm install`)
- [ ] `.env` файл заполнен и валиден
- [ ] База данных доступна (папка `data/` существует)
- [ ] Сессии доступны (папка `sessions/` существует)
- [ ] Бот запускается без ошибок
- [ ] Бот отвечает на `/start`
- [ ] Меню открываются корректно
- [ ] Логи выглядят нормально
- [ ] Нет критических ошибок в консоли

## 🎯 Следующие шаги

После успешного запуска:
1. Протестируйте все функции
2. Проверьте работу платежей (если возможно)
3. Проверьте работу VMManager интеграции
4. Настройте мониторинг (PM2 или Docker)
5. Подготовьте к production деплою

Удачи! 🚀

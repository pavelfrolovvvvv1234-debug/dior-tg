# Рефакторинг: Статус и План

## ✅ Выполнено (A, C, частично B, E)

### A) РЕПОЗИТОРИЙ / ГИГИЕНА ✅
- ✅ Обновлен `.gitignore` (добавлены все необходимые исключения)
- ✅ Обновлен `README.md` (детальная документация по установке, деплою, структуре)
- ⚠️ `.env.example` не создан (заблокирован системой)

### C) CONFIG / ENV ✅
- ✅ Создан `src/app/config.ts` с Zod валидацией всех переменных окружения
- ✅ Создан `src/app/logger.ts` для централизованного логирования
- ✅ Все env переменные валидируются на старте с понятными ошибками

### B) СТРУКТУРА ПРОЕКТА (частично) 🚧
Создана новая структура:
```
src/
├── app/                      ✅
│   ├── config.ts            ✅ Валидация env через Zod
│   ├── logger.ts            ✅ Централизованное логирование
│   ├── bot.ts               ⏳ НУЖНО: Инициализация grammY
│   └── server.ts            ⏳ НУЖНО: Webhook сервер (опционально)
│
├── domain/                   ⏳ НУЖНО: Бизнес-логика
│   ├── billing/             ⏳ PaymentService, BillingService
│   ├── services/            ⏳ VdsService, DomainService
│   └── users/               ⏳ UserService, AuthService
│
├── infrastructure/           🚧
│   ├── db/                  ✅
│   │   └── datasource.ts   ✅ DataSource с правильной инициализацией
│   ├── payments/            ✅
│   │   ├── types.ts        ✅ Интерфейс IPaymentProvider
│   │   ├── aaio.ts         ✅ AAIO адаптер
│   │   ├── crystalpay.ts   ✅ CrystalPay адаптер (временно импортирует старый)
│   │   └── factory.ts      ✅ Фабрика провайдеров
│   └── vmmanager/           ⏳ НУЖНО: Перенести VMManager с улучшениями
│
├── ui/                       ⏳ НУЖНО: Telegram UI слой
│   ├── screens/             ⏳ Screen Renderer (единый стиль сообщений)
│   ├── menus/               ⏳ Grammy menu definitions
│   └── components/          ⏳ Кнопки, шаблоны, форматеры
│
└── shared/                   ✅
    ├── types/               ✅ AppContext, SessionData
    └── errors/              ✅ Custom error classes
```

### E) PAYMENTS (частично) ✅
- ✅ Создан интерфейс `IPaymentProvider` для абстракции провайдеров
- ✅ Реализован `AAIOProvider` как адаптер
- ✅ Реализован `CrystalPayProvider` как адаптер (временно использует старый класс)
- ✅ Создана фабрика провайдеров
- ⏳ НУЖНО: `BillingService` для управления оплатами, создания инвойсов, проверки статуса, применения баланса

## ⏳ В ПРОЦЕССЕ / ТРЕБУЕТ ЗАВЕРШЕНИЯ

### D) DB / TYPEORM 🚧
- ✅ DataSource перенесен в `infrastructure/db/datasource.ts`
- ⏳ НУЖНО: Репозитории для каждой Entity (UserRepository, VdsRepository, DomainRepository, TopUpRepository, PromoRepository)
- ⏳ НУЖНО: Транзакции для критических операций (создание VDS, оплата)
- ⏳ НУЖНО: Миграции TypeORM (или четкая автоинициализация)

### F) TELEGRAM UX ⏳
- ⏳ НУЖНО: Screen Renderer (`ui/screens/renderer.ts`)
  - Единая функция `render(ctx, screenName, data) => {text, keyboard, parse_mode}`
  - Все тексты через i18n (Fluent)
- ⏳ НУЖНО: Единый стиль сообщений:
  - Заголовок
  - Короткое описание
  - Текущие параметры (баланс, статус)
  - Действия (кнопки)
- ⏳ НУЖНО: "Мягкие анимации":
  - `ctx.api.sendChatAction(chatId, "typing")` перед тяжелыми операциями
  - `editMessageText` вместо новых сообщений
  - Прогресс-строки: "⏳ Creating invoice..."
- ⏳ НУЖНО: Везде кнопки Back/Cancel
- ⏳ НУЖНО: Confirm на опасных действиях (reinstall OS, delete service)

### G) ОШИБКИ / НАДЕЖНОСТЬ ⏳
- ✅ Созданы custom error classes
- ⏳ НУЖНО: Глобальный error handler для grammY:
  ```typescript
  bot.catch((err) => {
    // Логировать
    // Показывать пользователю дружелюбное сообщение
    // Кнопка "В меню"
  });
  ```
- ⏳ НУЖНО: Retry для внешних API (VMManager, Payments) с axios-retry или кастомная логика
- ⏳ НУЖНО: Rate-limiting на чувствительные команды

### H) ТЕСТЫ / КАЧЕСТВО ⏳
- ⏳ НУЖНО: ESLint + Prettier конфигурация
- ⏳ НУЖНО: Unit-тесты:
  - config validation
  - payment provider adapters (моки)
  - форматирование сообщений
  - ключевые сценарии: create invoice -> status -> apply balance
- ⏳ НУЖНО: Scripts в package.json: `lint`, `test`, `typecheck`, `format`

### I) PRODUCTION ⏳
- ⏳ НУЖНО: Dockerfile
- ⏳ НУЖНО: docker-compose.yml (с volume для SQLite)
- ⏳ НУЖНО: Обновить ecosystem.config.js для PM2
- ⏳ НУЖНО: Health endpoint для webhook режима

### 10) МИГРАЦИЯ ОСНОВНОГО КОДА ⏳
- ⏳ НУЖНО: Переписать `src/index.ts` → `src/app/bot.ts`:
  - Тонкий bootstrap файл
  - Импорт всех middleware из `ui/`
  - Инициализация i18n, session, menus
- ⏳ НУЖНО: Перенести всю логику из `src/index.ts`:
  - Меню → `ui/menus/`
  - Хелперы → `domain/` или `ui/` (в зависимости от логики)
  - Conversations → `ui/conversations/`
  - Admin команды → `domain/admin/` или `ui/commands/`
- ⏳ НУЖНО: Создать domain services:
  - `BillingService`: создание инвойсов, проверка статуса, применение баланса (с транзакциями)
  - `VdsService`: создание VDS, управление VDS, переустановка OS
  - `DomainService`: регистрация доменов, управление доменами
  - `UserService`: управление пользователями, баланс, промокоды

## 📋 ПЛАН ЗАВЕРШЕНИЯ МИГРАЦИИ

### Приоритет 1: Базовая функциональность (без поломки)
1. ✅ Конфиг и структура (выполнено)
2. 🔄 Создать domain services (BillingService, VdsService, DomainService)
3. 🔄 Создать repositories для всех Entity
4. 🔄 Перенести VMManager в `infrastructure/vmmanager/` с retry и error handling
5. 🔄 Создать Screen Renderer и единый стиль сообщений
6. 🔄 Мигрировать основные меню в новую структуру
7. 🔄 Переписать `index.ts` → `bot.ts` с использованием новой архитектуры

### Приоритет 2: UX улучшения
8. Добавить "анимации" (typing, editMessageText, прогресс-бары)
9. Добавить Back/Cancel/Confirm везде
10. Улучшить тексты и структуру сообщений

### Приоритет 3: Надежность
11. Глобальный error handler
12. Retry для внешних API
13. Rate-limiting
14. Транзакции для критических операций

### Приоритет 4: Качество кода
15. ESLint + Prettier
16. Unit-тесты
17. TypeScript strict без `any`

### Приоритет 5: Production готовность
18. Dockerfile + docker-compose
19. PM2 config обновление
20. Health endpoint

## 🔍 КРИТИЧЕСКИЕ МОМЕНТЫ

### Безопасность
- ✅ `.gitignore` настроен правильно
- ⚠️ Нужно убедиться, что старый `.env`, `data.db`, `sessions/` удалены из истории git (если были закоммичены)

### Функциональность
- ⚠️ **ВАЖНО**: При миграции нужно сохранить весь функционал:
  - Покупка услуг (VDS, домены)
  - Пополнение баланса (AAIO, CrystalPay)
  - Управление услугами
  - Промокоды
  - Админ команды
  - Локализация (ru/en)

### База данных
- ⚠️ Старая БД (`data.db`) должна работать с новой структурой
- ⚠️ TypeORM `synchronize: true` нужно заменить на миграции в production

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. **Создать domain services**:
   - `src/domain/billing/BillingService.ts` - управление платежами
   - `src/domain/services/VdsService.ts` - управление VDS
   - `src/domain/services/DomainService.ts` - управление доменами

2. **Создать repositories**:
   - `src/infrastructure/db/repositories/UserRepository.ts`
   - `src/infrastructure/db/repositories/VdsRepository.ts`
   - `src/infrastructure/db/repositories/DomainRepository.ts`
   - `src/infrastructure/db/repositories/TopUpRepository.ts`
   - `src/infrastructure/db/repositories/PromoRepository.ts`

3. **Перенести VMManager**:
   - `src/infrastructure/vmmanager/VMManager.ts` с retry логикой

4. **Создать Screen Renderer**:
   - `src/ui/screens/renderer.ts` - единый рендеринг экранов

5. **Мигрировать меню**:
   - Перенести все меню из `src/index.ts` и `src/helpers/` в `src/ui/menus/`

6. **Создать новый bot.ts**:
   - Тонкий bootstrap файл
   - Инициализация всех компонентов

## 🎯 ПРОВЕРКА ГОТОВНОСТИ

- [ ] Проект запускается с нуля по README
- [ ] Никаких секретов/баз/сессий/диста в репозитории
- [ ] Меню работает, локали работают
- [ ] Покупка/пополнение/проверка статуса работают как раньше
- [ ] Управление услугами и VMManager операции работают
- [ ] Ошибки внешних API не валят бот, а красиво отображаются
- [ ] UX стал быстрее: меньше сообщений, больше editMessageText, везде Back/Cancel
- [ ] Код разделен по слоям: UI не ходит в БД напрямую, бизнес-логика тестируема
- [ ] TypeScript strict без any, без "портянок" в bot.ts

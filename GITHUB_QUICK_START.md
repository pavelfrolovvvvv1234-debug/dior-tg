# 🚀 Быстрый старт с GitHub

## ⚡ Самый простой способ загрузить на GitHub

### Вариант 1: Использовать автоматический скрипт (Windows) 🤖

```powershell
# Запустить скрипт настройки
.\setup-github.ps1
```

Скрипт автоматически:
- ✅ Проверит наличие Git
- ✅ Инициализирует репозиторий (если нужно)
- ✅ Добавит файлы
- ✅ Поможет создать коммит
- ✅ Подключит к GitHub
- ✅ Загрузит код на GitHub

### Вариант 2: Вручную 🔧

#### Шаг 1: Проверка текущего статуса

```bash
# Проверить статус Git
git status

# Проверить remote (если уже настроен)
git remote -v
```

#### Шаг 2: Добавить файлы (если есть изменения)

```bash
# Добавить все файлы
git add .

# Проверить что будет закоммичено
git status
```

#### Шаг 3: Создать коммит

```bash
# Если есть изменения, создать коммит
git commit -m "Refactored bot architecture with clean code structure"
```

#### Шаг 4: Создать репозиторий на GitHub

1. Перейдите на https://github.com/new
2. Создайте новый репозиторий (например, `drip-hosting-bot`)
3. **НЕ** добавляйте README, .gitignore или лицензию (у вас уже всё есть)
4. Нажмите "Create repository"

#### Шаг 5: Подключить к GitHub

```bash
# Если remote еще не настроен, добавьте его:
git remote add origin https://github.com/YOUR_USERNAME/drip-hosting-bot.git

# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код на GitHub
git push -u origin main
```

#### Шаг 6: Проверить что всё работает

1. Перейдите на ваш GitHub репозиторий
2. Проверьте, что все файлы загружены
3. Перейдите в **Actions** вкладку
4. Должен запуститься workflow "CI/CD Pipeline"
5. Дождитесь завершения (должна быть зеленая галочка ✅)

## 🔐 Настройка Secrets в GitHub

После загрузки кода на GitHub:

### 1. Перейдите в Settings

В вашем GitHub репозитории:
**Settings** → **Secrets and variables** → **Actions** → **New repository secret**

### 2. Добавьте все секреты из `.env`

**Обязательные секреты:**
```
BOT_TOKEN                    → токен Telegram бота
BOT_USERNAME                 → имя бота (без @)
WEBSITE_URL                  → URL сайта
SUPPORT_USERNAME_TG          → имя поддержки
PAYMENT_AAIO_ID              → AAIO ID
PAYMENT_AAIO_SECRET_ONE      → AAIO Secret 1
PAYMENT_AAIO_SECRET_TWO      → AAIO Secret 2
PAYMENT_AAIO_TOKEN           → AAIO Token
PAYMENT_CRYSTALPAY_ID        → CrystalPay ID
PAYMENT_CRYSTALPAY_SECRET_ONE → CrystalPay Secret 1
PAYMENT_CRYSTALPAY_SECRET_TWO → CrystalPay Secret 2
VMM_EMAIL                    → VMManager email
VMM_PASSWORD                 → VMManager password
VMM_ENDPOINT_URL             → VMManager endpoint URL
```

**Опциональные (для автоматического деплоя):**
```
SSH_HOST                     → SSH хост для деплоя
SSH_USER                     → SSH пользователь
SSH_KEY                      → SSH приватный ключ
SSH_PORT                     → SSH порт (по умолчанию 22)
DOCKER_USERNAME              → Docker Hub username
DOCKER_PASSWORD              → Docker Hub password
```

### 3. Проверьте что secrets добавлены

В **Settings** → **Secrets** должны быть все необходимые секреты.

## ✅ Что происходит после настройки

### Автоматические действия:

1. **При каждом push в main/master/develop:**
   - ✅ Запускается CI/CD Pipeline
   - ✅ Проверяется TypeScript компиляция
   - ✅ Запускается линтер
   - ✅ Проверяется форматирование
   - ✅ Собирается проект

2. **При pull request:**
   - ✅ Запускается Test Bot workflow
   - ✅ Проверяется валидация конфига
   - ✅ Тестируется сборка
   - ✅ Проверяются импорты модулей

3. **При push в main/master (если настроен деплой):**
   - ✅ Собирается deployment package
   - ✅ Автоматически деплоится через SSH
   - ✅ Собирается и публикуется Docker образ

## 🔍 Мониторинг GitHub Actions

### Просмотр результатов:

1. Перейдите в **Actions** вкладку на GitHub
2. Выберите workflow run (например, "CI/CD Pipeline")
3. Просмотрите логи каждого job:
   - ✅ Зеленая галочка = успешно
   - ❌ Красный крестик = ошибка
   - ⏳ Желтый кружок = выполняется

### Просмотр логов:

1. Кликните на workflow run
2. Кликните на job (например, "Test & Build")
3. Кликните на step для просмотра логов

## 🚨 Если что-то пошло не так

### Проблема: "Workflow failed"

1. Откройте логи workflow
2. Найдите ошибку (будет подсвечена красным)
3. Исправьте ошибку
4. Закоммитьте и запушьте изменения

### Проблема: "Secrets not found"

1. Проверьте, что все secrets добавлены в GitHub Settings
2. Проверьте названия secrets (должны совпадать с `.env`)
3. Убедитесь, что secrets добавлены для правильного репозитория

### Проблема: "Permission denied"

1. Проверьте права доступа к репозиторию
2. Убедитесь, что используете правильный токен/ключ
3. Проверьте, что SSH ключ добавлен в GitHub

## 📊 Статус workflows

После настройки вы увидите:

**В репозитории:**
- ✅ Badge статуса CI/CD (можно добавить в README)
- ✅ История всех workflow runs
- ✅ Логи выполнения

**В README можно добавить:**
```markdown
![CI/CD](https://github.com/YOUR_USERNAME/drip-hosting-bot/workflows/CI%2FCD%20Pipeline/badge.svg)
```

## 🎯 Чек-лист

- [ ] Git репозиторий инициализирован (уже есть ✅)
- [ ] Файлы добавлены (`git add .`)
- [ ] Коммит создан (`git commit`)
- [ ] Репозиторий создан на GitHub
- [ ] Remote добавлен (`git remote add origin`)
- [ ] Код отправлен на GitHub (`git push`)
- [ ] Secrets добавлены в GitHub Settings
- [ ] GitHub Actions работают (проверено в Actions)
- [ ] Все тесты проходят ✅

## 🚀 Готово!

Теперь ваш проект на GitHub и автоматически тестируется при каждом push!

**Дополнительные ресурсы:**
- `GITHUB_SETUP.md` - Детальная инструкция
- `GIT_COMMANDS.md` - Полезные Git команды
- `.github/workflows/` - Workflow файлы

**Удачи! 🎉**

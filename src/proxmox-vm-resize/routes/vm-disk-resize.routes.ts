/**
 * HTTP routes entry (thin layer; keeps routing discoverable in one folder).
 *
 * @module proxmox-vm-resize/routes/vm-disk-resize.routes
 */

export { createVmDiskResizeRouter } from "../controllers/vm-disk-resize.controller.js";

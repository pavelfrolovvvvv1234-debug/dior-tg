# 🚀 СТАРТ ЗДЕСЬ - Инструкция по запуску

## ⚡ Самый быстрый способ запустить

### 1. Проверьте, что всё готово:

```bash
# Запустите тест (Windows)
test.bat

# Или вручную:
npm install  # Установить зависимости
```

### 2. Проверьте .env файл:

Убедитесь, что `.env` файл существует и заполнен. Проверьте:
- `BOT_TOKEN` - токен вашего бота
- `BOT_USERNAME` - имя бота (без @)
- Все остальные переменные из `src/app/config.ts`

### 3. Запустите бота:

#### Вариант A: Старый код (рекомендуется для начала) ✅

```bash
# Windows - используйте готовый скрипт:
start.bat

# Или вручную:
npm run dev
```

#### Вариант B: Новый код (новая архитектура) 🆕

```bash
# Windows - используйте готовый скрипт:
start-new.bat

# Или вручную:
# 1. Создайте backup старого index.ts
copy src\index.ts src\index.old.ts

# 2. Используйте новый файл
copy src\index.new.ts src\index.ts

# 3. Запустите
npm run dev
```

## 📊 Что должно произойти

### При успешном запуске вы увидите:

**Старый код:**
```
[DripHosting Bot]: Starting
[DripHosting Bot]: Started
```

**Новый код:**
```
[INFO] Initializing bot...
[INFO] Fluent i18n initialized
[INFO] Database initialized
[INFO] VMManager initialized
[INFO] Services initialized
[INFO] PaymentStatusChecker initialized
[INFO] Bot initialized successfully
[INFO] PaymentStatusChecker started
[INFO] ExpirationService started
[INFO] Starting bot...
[INFO] Bot started in long polling mode
[INFO] Bot started successfully
```

### ❌ Если видите ошибки:

**"Invalid environment configuration"**
→ Проверьте `.env` файл - все переменные должны быть заполнены

**"Cannot find module"**
→ Запустите `npm install`

**"Database error"**
→ Проверьте, что папка `data/` существует и доступна для записи

**"Failed to authenticate with VMManager"**
→ Проверьте `VMM_EMAIL`, `VMM_PASSWORD`, `VMM_ENDPOINT_URL` в `.env`

## 🧪 Как протестировать

### 1. Откройте Telegram
   - Найдите вашего бота
   - Отправьте `/start`

### 2. Должно появиться:
   - ✅ Приветственное сообщение
   - ✅ Меню с кнопками:
     - 🧑‍💼 Profile
     - 💳 Purchase service
     - 🛠 Manage services
     - 🤝 Support
     - 📖 About us

### 3. Проверьте меню:
   - ✅ Все кнопки должны работать
   - ✅ Меню должны открываться
   - ✅ Сообщения должны отображаться правильно

### 4. Тестирование функций:

**Профиль:**
- Нажмите "🧑‍💼 Profile"
- Должен открыться профиль с балансом

**Пополнение баланса:**
- Нажмите "📤 Top up" в профиле
- Выберите сумму
- Выберите платежную систему
- Должна появиться ссылка на оплату

⚠️ **ВНИМАНИЕ:** При реальной оплате деньги будут списаны!

**Покупка VDS:**
- Нажмите "💳 Purchase service"
- Выберите "🖥 Virtual Dedicated Server (VDS)"
- Выберите тариф и ОС
- Подтвердите покупку

⚠️ **ВНИМАНИЕ:** При реальной покупке деньги будут списаны!

## 🔍 Мониторинг работы

### В консоли должны быть:
- ✅ Регулярные логи о работе сервисов
- ✅ Логи о проверке платежей (каждые 10 секунд)
- ✅ Логи о проверке истечения услуг (каждый день)
- ❌ НЕ должно быть критических ошибок

### В Telegram должно быть:
- ✅ Бот отвечает на команды
- ✅ Меню работают корректно
- ✅ Сообщения отображаются правильно
- ✅ Кнопки работают

## 🐛 Быстрое решение проблем

### Проблема: Bot не запускается

```bash
# 1. Проверьте зависимости
npm install

# 2. Проверьте .env
cat .env  # или Get-Content .env в PowerShell

# 3. Проверьте Node.js версию (должна быть 18+)
node --version
```

### Проблема: "TypeScript errors"

```bash
# Проверить ошибки
npm run typecheck

# Если много ошибок - используйте старый код (start.bat)
```

### Проблема: База данных заблокирована

```bash
# Закрыть все процессы Node.js
taskkill /F /IM node.exe

# Перезапустить
npm run dev
```

### Проблема: Bot не отвечает в Telegram

1. Проверьте `BOT_TOKEN` в `.env`
2. Убедитесь, что бот запущен (проверьте логи)
3. Проверьте интернет соединение
4. Перезапустите бота

## ✅ Чек-лист успешного запуска

- [ ] `.env` файл существует и заполнен
- [ ] Зависимости установлены (`npm install`)
- [ ] Папка `data/` существует
- [ ] Папка `sessions/` существует
- [ ] Node.js 18+ установлен
- [ ] Бот запускается без ошибок
- [ ] Бот отвечает на `/start` в Telegram
- [ ] Меню работают корректно
- [ ] Нет критических ошибок в консоли

## 📚 Дополнительные ресурсы

- `HOW_TO_RUN.md` - Детальная инструкция
- `QUICK_START.md` - Быстрый старт
- `DEPLOYMENT.md` - Деплой в production
- `README.md` - Основная документация

## 🎯 Следующие шаги

После успешного запуска:
1. ✅ Протестируйте все функции
2. ✅ Проверьте работу платежей (если возможно)
3. ✅ Проверьте работу VMManager интеграции
4. ✅ Настройте мониторинг (PM2 или Docker)
5. ✅ Подготовьте к production деплою

**Удачи! 🚀**

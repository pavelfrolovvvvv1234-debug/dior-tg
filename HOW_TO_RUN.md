# 🚀 Как запустить и протестировать проект

## 📋 Быстрый старт

### Вариант 1: Использовать готовые скрипты (Windows)

```bash
# Тест перед запуском
test.bat

# Запустить старый код (рекомендуется для начала)
start.bat

# Запустить новый код (новая архитектура)
start-new.bat
```

### Вариант 2: Ручной запуск

#### 1. Проверка зависимостей

```bash
# Проверить, что все зависимости установлены
npm install
# или
pnpm install
```

#### 2. Проверка .env файла

Убедитесь, что `.env` существует и содержит все переменные:
- `BOT_TOKEN` - токен бота
- `BOT_USERNAME` - имя бота
- `WEBSITE_URL` - URL сайта
- `SUPPORT_USERNAME_TG` - имя поддержки
- Credentials для платежных систем (AAIO, CrystalPay)
- Credentials для VMManager
- И другие переменные из `src/app/config.ts`

#### 3. Запуск старого кода (index.ts)

```bash
# Режим разработки с hot-reload
npm run dev

# Или сборка и запуск
npm run build
npm start
```

#### 4. Запуск нового кода (index.new.ts)

**Вариант A: Временно переименовать файлы**

```bash
# Создать backup старого index.ts
copy src\index.ts src\index.old.ts

# Переименовать новый файл
copy src\index.new.ts src\index.ts

# Запустить
npm run dev
```

**Вариант B: Использовать напрямую**

```bash
# Запустить напрямую через ts-node
npx ts-node src/index.new.ts
```

## 🧪 Тестирование

### Базовое тестирование

1. **Откройте Telegram** и найдите вашего бота
2. **Отправьте `/start`** - должно появиться приветственное сообщение
3. **Проверьте меню:**
   - ✅ "🧑‍💼 Profile" - открывает профиль
   - ✅ "💳 Purchase service" - открывает меню покупки услуг
   - ✅ "🛠 Manage services" - открывает управление услугами
   - ✅ "🤝 Support" - открывает поддержку
   - ✅ "📖 About us" - открывает информацию о нас

### Тестирование пополнения баланса

1. Откройте "🧑‍💼 Profile"
2. Нажмите "📤 Top up"
3. Выберите сумму (например, "10$")
4. Выберите платежную систему (AAIO или CrystalPay)
5. Должна появиться ссылка на оплату

**⚠️ ВНИМАНИЕ:** При реальной оплате деньги будут списаны!

### Тестирование покупки VDS

1. Откройте "💳 Purchase service"
2. Выберите "🖥 Virtual Dedicated Server (VDS)"
3. Выберите тариф
4. Выберите ОС
5. Подтвердите покупку

**⚠️ ВНИМАНИЕ:** При реальной покупке деньги будут списаны и VDS будет создан!

### Тестирование админ-команд (если вы админ)

```bash
# В Telegram боте:
/help          # Список команд
/users         # Список пользователей
/domainrequests # Список запросов доменов
/promo_codes   # Список промокодов
/create_promo  # Создать промокод
```

## 📊 Что должно быть в логах

### При успешном запуске старого кода (index.ts):

```
[DripHosting Bot]: Starting
[DripHosting Bot]: Started
```

### При успешном запуске нового кода (index.new.ts):

```
[INFO] Initializing bot...
[INFO] Fluent i18n initialized
[INFO] Database initialized
[INFO] VMManager initialized
[INFO] Services initialized
[INFO] PaymentStatusChecker initialized
[INFO] Bot initialized successfully
[INFO] PaymentStatusChecker started
[INFO] ExpirationService started
[INFO] Starting bot...
[INFO] Bot started in long polling mode
[INFO] Bot started successfully
```

### Если есть ошибки:

**❌ Invalid environment configuration:**
- Проверьте `.env` файл
- Все переменные должны быть заполнены

**❌ Failed to authenticate with VMManager:**
- Проверьте `VMM_EMAIL` и `VMM_PASSWORD`
- Проверьте `VMM_ENDPOINT_URL`

**❌ Payment provider error:**
- Проверьте credentials платежных систем
- Проверьте доступность API платежных систем

**❌ Database error:**
- Убедитесь, что папка `data/` существует
- Проверьте права на запись

## 🔍 Мониторинг работы

### Проверка в консоли:

```bash
# Должны быть регулярные логи:
[INFO] Checking payment statuses...
[INFO] Checking expired services...
```

### Проверка в Telegram:

- ✅ Бот отвечает на команды
- ✅ Меню открываются корректно
- ✅ Сообщения отображаются правильно
- ✅ Кнопки работают

### Проверка базы данных:

```bash
# Проверить размер базы (должна расти при использовании)
dir data.db

# Проверить файлы сессий (должны создаваться для каждого пользователя)
dir sessions\ /s
```

## 🐛 Решение проблем

### Проблема: "Cannot find module"

```bash
# Переустановить зависимости
rmdir /s /q node_modules
npm install
```

### Проблема: "TypeScript compilation errors"

```bash
# Проверить ошибки компиляции
npm run typecheck

# Если есть ошибки, исправить их или использовать старый код
```

### Проблема: Bot не отвечает

1. Проверьте `BOT_TOKEN` в `.env`
2. Убедитесь, что бот запущен (проверьте логи)
3. Проверьте интернет соединение
4. Проверьте, что бот не заблокирован

### Проблема: База данных заблокирована

```bash
# Закрыть все процессы Node.js
taskkill /F /IM node.exe

# Удалить lock файлы (осторожно!)
del data.db-shm
del data.db-wal
```

## ✅ Чек-лист перед запуском

- [ ] `.env` файл создан и заполнен
- [ ] Зависимости установлены (`npm install`)
- [ ] Папка `data/` существует
- [ ] Папка `sessions/` существует
- [ ] Node.js версии 18+ установлен
- [ ] Бот создан в Telegram BotFather
- [ ] Токен бота получен и добавлен в `.env`

## 🎯 Рекомендации

1. **Начните со старого кода** (`start.bat` или `npm run dev`) - он точно работает
2. **Тестируйте базовые функции** перед тестированием платежей
3. **Делайте backup базы** перед критическими тестами:
   ```bash
   copy data.db data.db.backup
   ```
4. **Следите за логами** - там будут все ошибки
5. **Используйте тестовые аккаунты** для платежных систем

## 📚 Дополнительные ресурсы

- `README.md` - Основная документация
- `DEPLOYMENT.md` - Руководство по деплою
- `QUICK_START.md` - Быстрый старт
- `FINAL_SUMMARY.md` - Итоговая сводка рефакторинга

## 🚀 Следующие шаги

После успешного запуска и тестирования:
1. ✅ Проверьте все функции
2. ✅ Убедитесь, что всё работает корректно
3. ✅ Настройте мониторинг (PM2 или Docker)
4. ✅ Подготовьте к production деплою

**Удачи! 🎉**
